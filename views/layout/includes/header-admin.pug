header.bg-blue-600.p-2
    .container.mx-auto.flex.justify-between.items-center
        a(href="/")
            h1.text-2xl.text-white.font-extrabold.text-center Bienes
                span.font-normal.ml-2 Raíces
        nav(class="my-5 text-sm md:flex md:items-center font-bold text-white hidden")
            a(href="/my-properties" class="capitalize inline-block hover:bg-blue-800 transition py-2 px-3 rounded-lg") mis propiedades
            form(action="/auth/logout" method="POST")
                input(type="hidden" name="_csrf" value=csrfToken)
                input(type="submit" class="capitalize inline-block ml-10 bg-blue-800 hover:bg-blue-500 transition py-2 px-10 rounded-lg cursor-pointer" value="Cerrar Sesión")