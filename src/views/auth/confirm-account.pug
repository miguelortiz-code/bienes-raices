extends ../layout/index

block contenido
  .py-10
    h1(class="text-4xl my-10 font-extrabold text-center") Bienes
      span(class="font-normal")  Raices
    h2(class="text-center text-2xl font-extrabold mb-3")= pagina

    div(class="bg-white shadow py-8 px-4 rounded-lg max-w-md mx-auto")
      p(class= (error ? "bg-red-500" : "bg-green-500") + " transition duration-300").px-5.py-4.text-white.rounded-lg.w-full.font-bold.text-center.uppercase= mensaje

    
      if !error
          a(href="/auth/login" class="text-center font-bold text-sm text-slate-600 uppercase mt-4 block") Iniciar Sesión 